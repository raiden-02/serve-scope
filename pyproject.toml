[project]
name = "serve-scope"
version = "0.0.1"
description = "Interactive LLM inference lab. P0 only proves local GPU-backed serving."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "vllm==0.28.0",
]
